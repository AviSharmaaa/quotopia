type LogInSchema = {
    username: string
    otp: string
}

type Quote = {
    createdAt: string;
    id: number;
    mediaUrl: string;
    text: string;
    updatedAt: string;
    username: string;
};
